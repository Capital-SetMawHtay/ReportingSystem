<%= form_for(user,method: :put,html: {class: 'team_transfer_form'}) do |f|%>
    <%= f.select(:team_id,Team.all.map{|t|[t.name,t.id]})%>
    <%= f.submit 'Transfer',class: 'button tiny alert radius' %>
<%end%>